rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write for all users (since auth is handled by Supabase)
      allow read, write: if true;
    }
  }
}
